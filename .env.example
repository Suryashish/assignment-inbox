# ─── Web (exposed to the browser, must be a PUBLIC url) ───────────────
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# ─── Server ───────────────────────────────────────────────────────────
PORT=4000
# Inside docker-compose this is rewritten to redis://redis:6379
REDIS_URL=redis://localhost:6379
CORS_ORIGIN=http://localhost:3000
